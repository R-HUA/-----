{
  "name": "service-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Unified web service dashboard with backend reverse proxy and tabbed frontend workspace.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
